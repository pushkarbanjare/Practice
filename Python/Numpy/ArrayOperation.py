# Given two arrays, a = np.array([1, 2, 3]) and b = np.array([4, 5, 6]), perform element-wise addition, subtraction, multiplication, and division.

import numpy as np

a = np.array([1,2,3])
b = np.array([4,5,6])

add = a+b
sub = a-b
mul = a*b
div = a/b

print(add)
print(sub)
print(mul)
print(div)